1.lexer词法分析
    1.1 定义所有可能出现的token✅
    1.2 定义每个token的正则表达式✅
        单引号和双引号都表示字符串✅
        表名称或列名称：以字母或下划线开头，后跟字母、数字或下划线✅
        关键字不区分大小写✅
        列添加格式，例如string(10),int(10),float(10)
        实体完整性，参照完整性，用户自定义完整性
    1.3 SQL语句不区分大小写
    1.4 忽略空格、制表符、换行符✅
2. parser语法分析
3.前端
    3.1 输入SQL语句
    3.2 点击执行按钮
        有一个执行按钮，还有一个执行状态单元，默认为黑圈白底，点击后变为黑圈黑底，表示正在执行，
        执行成功显示绿色，错误时显示红色。执行完成返回这条SQL语句的执行时间。
    3.3 显示执行结果
        保留最近10条SQL语句以及执行结果
    3.4 SQL命令示例
4.后端
    4.1 接收前端请求
    4.2 调用lexer和parser进行处理
    4.3 返回处理结果
